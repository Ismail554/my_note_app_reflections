import 'package:flutter/material.dart';

class AppColors {
  // ─── Primary Palette (Forest Green) ───────────────────────────────────────
  static const Color primaryDark = Color(0xFF1A5340);
  static const Color primaryMedium = Color(0xFF2D7A5A);
  static const Color primaryLight = Color(0xFF4FAB7E);
  static const Color primaryXLight = Color(0xFFA8D5BF);
  static const Color primarySurface = Color(0xFFE2EFE8);

  // ─── Backgrounds ──────────────────────────────────────────────────────────
  static const Color background = Color(0xFFF4F6F4);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFE8EDE8);
  static const Color cardBackground = Color(0xFFECF0EC);

  // ─── Text ─────────────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF3D5A48);
  static const Color textMuted = Color(0xFF7A9A88);
  static const Color textHint = Color(0xFF9AB5A5);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // ─── UI Elements ──────────────────────────────────────────────────────────
  static const Color divider = Color(0xFFD8E4DC);
  static const Color chipBackground = Color(0xFFD6EAE0);
  static const Color chipBorder = Color(0xFFB2D4C2);
  static const Color inputBorder = Color(0xFFCDD9D3);
  static const Color inputFill = Color(0xFFF8FAF8);

  // ─── Status ───────────────────────────────────────────────────────────────
  static const Color error = Color(0xFFB85450);
  static const Color errorLight = Color(0xFFF8ECEC);
  static const Color success = Color(0xFF2D7A5A);

  // ─── Neutral ──────────────────────────────────────────────────────────────
  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);
  static const Color transparent = Colors.transparent;
}